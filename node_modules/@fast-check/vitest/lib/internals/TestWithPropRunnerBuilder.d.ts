import type { Parameters as FcParameters } from 'fast-check';
import type { Prop, It, ArbitraryTuple, FcExtra } from './types.js';
export declare function buildTestWithPropRunner<Ts extends [any] | any[], TsParameters extends Ts = Ts>(testFn: It | It['only' | 'skip' | 'concurrent'] | It['concurrent']['only' | 'skip'], label: string, arbitraries: ArbitraryTuple<Ts>, prop: Prop<Ts>, params: FcParameters<TsParameters> | undefined, timeout: number | undefined, fc: FcExtra): void;
